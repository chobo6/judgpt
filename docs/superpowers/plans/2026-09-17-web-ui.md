# judgpt Web UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 기존 CLI(`judgpt/analyze.py`)의 탐지/법률 RAG 로직을 그대로 재사용하는 FastAPI 백엔드 + React SPA 프론트엔드를 추가해, 계정 없이 브라우저에서 채팅 텍스트를 붙여넣고 바로 결과를 볼 수 있게 한다.

**Architecture:** `judgpt/web/app.py`의 `POST /api/analyze` 엔드포인트가 `judgpt.analyzer.analyze()`/`judgpt.legal_rag.enrich()`를 직접 호출한다(`judgpt.analyze.run()`은 `AnalysisError`를 `SystemExit`으로 바꿔버려 서버에서 못 쓴다 — spec §3 참고). LLM/Embedder는 앱 시작 시 한 번 생성해 재사용하고(`judgpt/web/dependencies.py`), FastAPI `Depends`로 주입해 테스트에서 `FakeLLM`/`FakeEmbedder`로 교체 가능하게 한다. 프론트(`frontend/`, Vite+React)는 별도 Node 프로젝트로 이 API 하나만 호출한다.

**Tech Stack:** 백엔드는 기존 `openai`/`pydantic`에 `fastapi`, `uvicorn`, `slowapi`(rate limit), `httpx`(테스트용 `TestClient`)를 새로 추가. 프론트는 Vite + React(JavaScript, TypeScript 아님 — 이 규모에 TS 설정 오버헤드가 불필요하다고 판단).

**Spec:** `docs/superpowers/specs/2026-09-17-web-ui-design.md`

## Global Constraints

- `judgpt/analyzer.py`, `judgpt/schema.py`, `judgpt/llm.py`, `judgpt/legal_rag.py`, `judgpt/report.py`, `judgpt/embedder.py`, `judgpt/analyze.py`의 기존 로직은 수정하지 않는다.
- 탐지/법률 RAG 로직을 웹 백엔드에 재구현하지 않는다 — `judgpt.analyzer.analyze()`/`judgpt.legal_rag.enrich()`를 직접 호출한다. `judgpt.analyze.run()`은 쓰지 않는다(`AnalysisError` → `SystemExit` 변환이 서버 핸들러에 안 맞음).
- 계정/로그인, 서버 측 결과 저장(로그 포함), 프론트 로컬스토리지 저장을 추가하지 않는다.
- `DISCLAIMER`와 동일한 한국어 고지 문구는 프론트에 고정 문구로 존재해야 하며 항상 표시된다.
- rate limit은 IP당 분당 5회, `slowapi` 사용.
- API 에러 응답 형식은 정확히 `{"detail": "..."}`이고, 상태 코드는 400(빈 입력)/429(rate limit)/502(`AnalysisError`)/503(`APIConnectionError`)다.
- eval 하네스(`judgpt/eval.py`)는 웹 UI 범위에서 제외한다.
- 실제 배포(Dockerfile 이후의 k8s/클라우드 배포)는 이 계획의 범위가 아니다.

---

## File Structure

```
judgpt/
  web/
    __init__.py             # 신규 (빈 파일)
    dependencies.py           # 신규: get_llm(), get_embedder() — 앱 전역 싱글턴
    app.py                      # 신규: FastAPI 앱, POST /api/analyze, rate limit, 정적 프론트 서빙
frontend/
  package.json                  # 신규: Vite + React
  vite.config.js                 # 신규: dev 프록시(/api → localhost:8000)
  index.html                      # 신규
  src/
    main.jsx                      # 신규: React 마운트
    App.jsx                        # 신규: 입력 폼 + 결과 렌더링 (단일 화면)
    api.js                          # 신규: POST /api/analyze 호출 래퍼 + ApiError
tests/
  conftest.py                      # 신규: TestClient fixture (dependency_overrides/rate limit 초기화)
  test_web_api.py                   # 신규
Dockerfile                          # 신규: 백엔드 + 빌드된 프론트 정적 파일 컨테이너화
pyproject.toml                       # 수정: [project.optional-dependencies]에 web 추가
```

---

### Task 1: FastAPI 앱 기본 골격 + 탐지 전용(legal=false) 엔드포인트

**Files:**
- Modify: `pyproject.toml`
- Create: `judgpt/web/__init__.py` (빈 파일)
- Create: `judgpt/web/dependencies.py`
- Create: `judgpt/web/app.py`
- Create: `tests/conftest.py`
- Test: `tests/test_web_api.py`

**Interfaces:**
- Consumes: `judgpt.config.MODEL`/`OLLAMA_BASE_URL` (기존), `judgpt.llm.LLM`/`OllamaLLM`/`FakeLLM` (기존), `judgpt.analyzer.analyze(chat_text, llm) -> AnalysisResult`/`AnalysisError` (기존), `judgpt.schema.AnalysisResult` (기존)
- Produces: `judgpt.web.dependencies.get_llm() -> LLM`, `judgpt.web.dependencies.get_embedder() -> Embedder`, `judgpt.web.app.app`(FastAPI 인스턴스), `POST /api/analyze`(이 태스크에서는 `legal` 필드를 받되 아직 무시하고 항상 탐지 전용 응답만 반환 — Task 2에서 실제 분기 추가)

- [ ] **Step 1: `pyproject.toml`에 `web` extra 추가**

`[project.optional-dependencies]` 섹션을 아래로 교체:

```toml
[project.optional-dependencies]
dev = ["pytest>=8.0"]
web = ["fastapi>=0.115", "uvicorn[standard]>=0.30", "slowapi>=0.1.9", "httpx>=0.27"]
```

설치: `pip install -e ".[web,dev]"`

- [ ] **Step 2: `judgpt/web/__init__.py` 생성 (빈 파일)**

- [ ] **Step 3: 실패하는 테스트 작성 — `tests/test_web_api.py`**

```python
from judgpt.embedder import OllamaEmbedder
from judgpt.llm import OllamaLLM
from judgpt.web.dependencies import get_embedder, get_llm


def test_get_llm_returns_same_instance_on_repeated_calls():
    first = get_llm()
    second = get_llm()

    assert first is second
    assert isinstance(first, OllamaLLM)


def test_get_embedder_returns_same_instance_on_repeated_calls():
    first = get_embedder()
    second = get_embedder()

    assert first is second
    assert isinstance(first, OllamaEmbedder)
```

- [ ] **Step 4: 테스트 실행 — 실패 확인**

Run: `pytest tests/test_web_api.py -v`
Expected: FAIL with `ModuleNotFoundError: No module named 'judgpt.web'`

- [ ] **Step 5: `judgpt/web/dependencies.py` 구현**

```python
from judgpt import config
from judgpt.embedder import Embedder, OllamaEmbedder
from judgpt.llm import LLM, OllamaLLM

_llm: LLM | None = None
_embedder: Embedder | None = None


def get_llm() -> LLM:
    global _llm
    if _llm is None:
        _llm = OllamaLLM(model=config.MODEL, base_url=config.OLLAMA_BASE_URL)
    return _llm


def get_embedder() -> Embedder:
    global _embedder
    if _embedder is None:
        _embedder = OllamaEmbedder(model=config.EMBEDDING_MODEL, base_url=config.OLLAMA_BASE_URL)
    return _embedder
```

- [ ] **Step 6: 테스트 실행 — 통과 확인**

Run: `pytest tests/test_web_api.py -v`
Expected: PASS (2 passed)

- [ ] **Step 7: `tests/conftest.py` 작성**

```python
import pytest
from fastapi.testclient import TestClient

from judgpt.web.app import app


@pytest.fixture
def client():
    yield TestClient(app)
    app.dependency_overrides.clear()
```

- [ ] **Step 8: 실패하는 테스트 작성 — `tests/test_web_api.py`에 추가 (파일 끝)**

```python
from judgpt.llm import FakeLLM
from judgpt.web.app import app
from judgpt.web.dependencies import get_llm


def test_analyze_endpoint_returns_expressions(client):
    fake_llm = FakeLLM(['{"expressions": []}'])
    app.dependency_overrides[get_llm] = lambda: fake_llm

    response = client.post("/api/analyze", json={"chat_text": "A: 안녕"})

    assert response.status_code == 200
    assert response.json() == {"expressions": []}


def test_analyze_endpoint_returns_400_on_empty_chat_text(client):
    response = client.post("/api/analyze", json={"chat_text": "   "})

    assert response.status_code == 400
    assert response.json() == {"detail": "입력이 비어 있습니다"}
```

- [ ] **Step 9: 테스트 실행 — 실패 확인**

Run: `pytest tests/test_web_api.py -v`
Expected: FAIL with `ModuleNotFoundError: No module named 'judgpt.web.app'`

- [ ] **Step 10: `judgpt/web/app.py` 구현**

```python
from fastapi import Depends, FastAPI, HTTPException
from pydantic import BaseModel

from judgpt.analyzer import AnalysisError, analyze
from judgpt.llm import LLM
from judgpt.schema import AnalysisResult
from judgpt.web.dependencies import get_llm

app = FastAPI()


class AnalyzeRequest(BaseModel):
    chat_text: str
    legal: bool = False


@app.post("/api/analyze")
def analyze_endpoint(
    body: AnalyzeRequest,
    llm: LLM = Depends(get_llm),
) -> AnalysisResult:
    if not body.chat_text.strip():
        raise HTTPException(status_code=400, detail="입력이 비어 있습니다")

    try:
        return analyze(body.chat_text, llm)
    except AnalysisError:
        raise HTTPException(status_code=502, detail="분석에 실패했습니다. 다시 시도해주세요")
```

- [ ] **Step 11: 테스트 실행 — 통과 확인**

Run: `pytest tests/test_web_api.py -v`
Expected: PASS (4 passed)

- [ ] **Step 12: 커밋**

```bash
git add pyproject.toml judgpt/web/__init__.py judgpt/web/dependencies.py judgpt/web/app.py tests/conftest.py tests/test_web_api.py
git commit -m "웹 백엔드 기본 골격 + 탐지 전용 POST /api/analyze 추가"
```

---

### Task 2: `--legal` 플래그 웹 연동

**Files:**
- Modify: `judgpt/web/app.py`
- Test: `tests/test_web_api.py` (파일 끝에 추가)

**Interfaces:**
- Consumes: `judgpt.legal_rag.enrich(result, embedder, *, is_online=False, cases=None) -> EnrichedResult` (기존), `judgpt.embedder.Embedder`/`FakeEmbedder` (기존), `judgpt.legal_data.cases.load_cases()` (기존), `judgpt.web.dependencies.get_embedder` (Task 1)
- Produces: `POST /api/analyze`가 `body.legal == True`일 때 `EnrichedResult`(적용 가능 법률/관련 판례 포함)를 반환

- [ ] **Step 1: 실패하는 테스트 작성 — `tests/test_web_api.py`에 추가 (파일 끝)**

```python
from judgpt.embedder import FakeEmbedder
from judgpt.legal_data.cases import load_cases
from judgpt.web.dependencies import get_embedder


def test_analyze_endpoint_with_legal_flag_returns_enriched_result(client):
    fake_llm = FakeLLM([
        '{"expressions": [{"text": "너는 쓰레기야", "type": "모욕", "risk": "높음"}]}'
    ])
    vectors = {case.summary: [0.1, 0.2, 0.3] for case in load_cases()}
    vectors["너는 쓰레기야"] = [0.1, 0.2, 0.3]
    fake_embedder = FakeEmbedder(vectors)
    app.dependency_overrides[get_llm] = lambda: fake_llm
    app.dependency_overrides[get_embedder] = lambda: fake_embedder

    response = client.post(
        "/api/analyze", json={"chat_text": "A: 예시", "legal": True}
    )

    assert response.status_code == 200
    data = response.json()
    assert data["expressions"][0]["applicable_laws"] == ["형법 제311조(모욕)"]
    assert "needs_verification" in data
```

- [ ] **Step 2: 테스트 실행 — 실패 확인**

Run: `pytest tests/test_web_api.py -v`
Expected: FAIL — `legal=True`여도 응답에 `applicable_laws`가 없음(`AssertionError` 또는 `KeyError`)

- [ ] **Step 3: `judgpt/web/app.py`를 아래 내용으로 교체**

```python
from fastapi import Depends, FastAPI, HTTPException
from pydantic import BaseModel

from judgpt.analyzer import AnalysisError, analyze
from judgpt.embedder import Embedder
from judgpt.legal_rag import EnrichedResult, enrich
from judgpt.llm import LLM
from judgpt.schema import AnalysisResult
from judgpt.web.dependencies import get_embedder, get_llm

app = FastAPI()


class AnalyzeRequest(BaseModel):
    chat_text: str
    legal: bool = False


@app.post("/api/analyze")
def analyze_endpoint(
    body: AnalyzeRequest,
    llm: LLM = Depends(get_llm),
    embedder: Embedder = Depends(get_embedder),
) -> AnalysisResult | EnrichedResult:
    if not body.chat_text.strip():
        raise HTTPException(status_code=400, detail="입력이 비어 있습니다")

    try:
        result = analyze(body.chat_text, llm)
        if body.legal:
            return enrich(result, embedder)
        return result
    except AnalysisError:
        raise HTTPException(status_code=502, detail="분석에 실패했습니다. 다시 시도해주세요")
```

- [ ] **Step 4: 테스트 실행 — 통과 확인**

Run: `pytest tests/test_web_api.py -v`
Expected: PASS (5 passed)

- [ ] **Step 5: 커밋**

```bash
git add judgpt/web/app.py tests/test_web_api.py
git commit -m "웹 API에 --legal 플래그(법률 RAG) 연동"
```

---

### Task 3: Ollama 연결 실패(503) 에러 매핑

**Files:**
- Modify: `judgpt/web/app.py`
- Test: `tests/test_web_api.py` (파일 끝에 추가)

**Interfaces:**
- Consumes: `openai.APIConnectionError` (기존 의존성), `judgpt.web.app.app`/`analyze_endpoint` (Task 1, 2)
- Produces: `POST /api/analyze`가 `APIConnectionError`(analyze() 또는 enrich()의 embedder.embed() 호출 중 발생) 발생 시 503 반환

- [ ] **Step 1: 실패하는 테스트 작성 — `tests/test_web_api.py`에 추가 (파일 끝)**

```python
def test_analyze_endpoint_returns_503_when_ollama_unreachable(client):
    import httpx
    from openai import APIConnectionError

    class _ConnectionErrorLLM:
        def call(self, messages):
            raise APIConnectionError(
                request=httpx.Request("POST", "http://localhost:11434/v1/chat/completions")
            )

    app.dependency_overrides[get_llm] = lambda: _ConnectionErrorLLM()

    response = client.post("/api/analyze", json={"chat_text": "A: 예시"})

    assert response.status_code == 503
    assert response.json() == {"detail": "분석 엔진이 응답하지 않습니다"}
```

- [ ] **Step 2: 테스트 실행 — 실패 확인**

Run: `pytest tests/test_web_api.py -v`
Expected: FAIL — `APIConnectionError`가 안 잡혀서 500(unhandled exception)이 남

- [ ] **Step 3: `judgpt/web/app.py`의 `analyze_endpoint` 함수를 아래로 교체**

```python
@app.post("/api/analyze")
def analyze_endpoint(
    body: AnalyzeRequest,
    llm: LLM = Depends(get_llm),
    embedder: Embedder = Depends(get_embedder),
) -> AnalysisResult | EnrichedResult:
    if not body.chat_text.strip():
        raise HTTPException(status_code=400, detail="입력이 비어 있습니다")

    try:
        result = analyze(body.chat_text, llm)
        if body.legal:
            return enrich(result, embedder)
        return result
    except AnalysisError:
        raise HTTPException(status_code=502, detail="분석에 실패했습니다. 다시 시도해주세요")
    except APIConnectionError:
        raise HTTPException(status_code=503, detail="분석 엔진이 응답하지 않습니다")
```

파일 맨 위 import 블록에 `from openai import APIConnectionError` 한 줄 추가(기존 `from fastapi import ...` 위 또는 아래 아무 곳이나, 다른 import와 함께 알파벳 순 유지):

```python
from openai import APIConnectionError
```

- [ ] **Step 4: 테스트 실행 — 통과 확인**

Run: `pytest tests/test_web_api.py -v`
Expected: PASS (6 passed)

- [ ] **Step 5: 커밋**

```bash
git add judgpt/web/app.py tests/test_web_api.py
git commit -m "Ollama 연결 실패 시 503으로 매핑"
```

---

### Task 4: IP당 분당 5회 Rate Limiting

**Files:**
- Modify: `judgpt/web/app.py`
- Modify: `tests/conftest.py`
- Test: `tests/test_web_api.py` (파일 끝에 추가)

**Interfaces:**
- Consumes: `slowapi.Limiter`, `slowapi.util.get_remote_address`, `slowapi.errors.RateLimitExceeded` (새 의존성, Task 1에서 설치됨)
- Produces: `judgpt.web.app.limiter`(모듈 레벨 `Limiter` 인스턴스 — 테스트에서 `reset()` 호출용), `/api/analyze`가 같은 클라이언트로부터 분당 5회 초과 요청 시 429 반환

- [ ] **Step 1: 실패하는 테스트 작성 — `tests/test_web_api.py`에 추가 (파일 끝)**

```python
def test_analyze_endpoint_rate_limited_after_five_requests_per_minute(client):
    fake_llm = FakeLLM(['{"expressions": []}'] * 5)
    app.dependency_overrides[get_llm] = lambda: fake_llm

    for _ in range(5):
        response = client.post("/api/analyze", json={"chat_text": "A: 안녕"})
        assert response.status_code == 200

    response = client.post("/api/analyze", json={"chat_text": "A: 안녕"})

    assert response.status_code == 429
    assert response.json() == {"detail": "요청이 너무 많습니다. 잠시 후 다시 시도하세요"}
```

- [ ] **Step 2: 테스트 실행 — 실패 확인**

Run: `pytest tests/test_web_api.py -v`
Expected: FAIL — 6번째 요청도 200으로 통과(아직 rate limit 없음)

- [ ] **Step 3: `judgpt/web/app.py` 전체를 아래 내용으로 교체**

```python
from fastapi import Depends, FastAPI, HTTPException, Request
from fastapi.responses import JSONResponse
from openai import APIConnectionError
from pydantic import BaseModel
from slowapi import Limiter
from slowapi.errors import RateLimitExceeded
from slowapi.util import get_remote_address

from judgpt.analyzer import AnalysisError, analyze
from judgpt.embedder import Embedder
from judgpt.legal_rag import EnrichedResult, enrich
from judgpt.llm import LLM
from judgpt.schema import AnalysisResult
from judgpt.web.dependencies import get_embedder, get_llm

app = FastAPI()

limiter = Limiter(key_func=get_remote_address)
app.state.limiter = limiter


@app.exception_handler(RateLimitExceeded)
def rate_limit_handler(request: Request, exc: RateLimitExceeded) -> JSONResponse:
    return JSONResponse(
        status_code=429,
        content={"detail": "요청이 너무 많습니다. 잠시 후 다시 시도하세요"},
    )


class AnalyzeRequest(BaseModel):
    chat_text: str
    legal: bool = False


@app.post("/api/analyze")
@limiter.limit("5/minute")
def analyze_endpoint(
    request: Request,
    body: AnalyzeRequest,
    llm: LLM = Depends(get_llm),
    embedder: Embedder = Depends(get_embedder),
) -> AnalysisResult | EnrichedResult:
    if not body.chat_text.strip():
        raise HTTPException(status_code=400, detail="입력이 비어 있습니다")

    try:
        result = analyze(body.chat_text, llm)
        if body.legal:
            return enrich(result, embedder)
        return result
    except AnalysisError:
        raise HTTPException(status_code=502, detail="분석에 실패했습니다. 다시 시도해주세요")
    except APIConnectionError:
        raise HTTPException(status_code=503, detail="분석 엔진이 응답하지 않습니다")
```

**중요**: `slowapi`의 `@limiter.limit(...)` 데코레이터가 요청의 클라이언트 IP를 읽으려면 라우트 함수가 `request: Request` 파라미터를 받아야 한다 — FastAPI가 이를 자동으로 주입한다(요청 본문 파싱과 무관, 순서상 `body` 앞이든 뒤든 상관없지만 관례상 첫 인자로 둔다).

- [ ] **Step 4: `tests/conftest.py`를 아래로 교체 (rate limit 상태를 테스트마다 초기화)**

```python
import pytest
from fastapi.testclient import TestClient

from judgpt.web.app import app, limiter


@pytest.fixture
def client():
    yield TestClient(app)
    app.dependency_overrides.clear()
    limiter.reset()
```

- [ ] **Step 5: 테스트 실행 — 통과 확인**

Run: `pytest tests/test_web_api.py -v`
Expected: PASS (7 passed)

- [ ] **Step 6: 전체 테스트 스위트 실행 — 회귀 확인**

Run: `pytest -q`
Expected: 기존 테스트 전부 + 신규 7개 PASS, 통합 테스트(`integration` 마커)는 기본 실행에서 제외.

- [ ] **Step 7: 커밋**

```bash
git add judgpt/web/app.py tests/conftest.py tests/test_web_api.py
git commit -m "IP당 분당 5회 rate limiting 추가"
```

---

### Task 5: React 프론트엔드 (Vite)

**Files:**
- Create: `frontend/package.json`
- Create: `frontend/vite.config.js`
- Create: `frontend/index.html`
- Create: `frontend/src/main.jsx`
- Create: `frontend/src/App.jsx`
- Create: `frontend/src/api.js`

**Interfaces:**
- Consumes: `POST /api/analyze`(Task 1-4에서 완성된 API 계약 — 성공 시 `{"expressions": [...]}` 또는 `{"expressions": [...], "needs_verification": bool}`, 실패 시 `{"detail": "..."}` + 400/429/502/503)
- Produces: 브라우저에서 여는 단일 화면 SPA. 자동화 테스트 없음(spec §6 — 이 프로젝트엔 JS 테스트 컨벤션이 없고 MVP 범위에서는 수동 확인으로 충분하다고 결정) — 이 태스크의 "테스트"는 개발 서버를 띄워 브라우저로 직접 확인하는 것이다.

- [ ] **Step 1: `frontend/package.json` 작성**

```json
{
  "name": "judgpt-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.0"
  }
}
```

- [ ] **Step 2: `frontend/vite.config.js` 작성**

개발 중 `npm run dev`(기본 포트 5173)에서 `/api`로 가는 요청을 백엔드(`uvicorn judgpt.web.app:app --port 8000`)로 프록시한다 — 프론트/백엔드를 따로 띄워도 CORS 설정 없이 바로 연동되도록.

```javascript
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

export default defineConfig({
  plugins: [react()],
  server: {
    proxy: {
      "/api": "http://localhost:8000",
    },
  },
});
```

- [ ] **Step 3: `frontend/index.html` 작성**

```html
<!doctype html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>judgpt</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.jsx"></script>
  </body>
</html>
```

- [ ] **Step 4: `frontend/src/api.js` 작성**

```javascript
export class ApiError extends Error {
  constructor(status, message) {
    super(message);
    this.status = status;
  }
}

export async function analyzeChat(chatText, legal) {
  const response = await fetch("/api/analyze", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ chat_text: chatText, legal }),
  });

  if (!response.ok) {
    const body = await response.json().catch(() => ({}));
    throw new ApiError(response.status, body.detail || "알 수 없는 오류가 발생했습니다");
  }

  return response.json();
}
```

- [ ] **Step 5: `frontend/src/App.jsx` 작성**

```jsx
import { useState } from "react";
import { analyzeChat, ApiError } from "./api";

const DISCLAIMER =
  "이 결과는 참고용 정보이며 법적 판단이 아닙니다. 실제 법적 대응이 필요하면 변호사와 상담하세요.";

function errorMessage(err) {
  if (!(err instanceof ApiError)) return "알 수 없는 오류가 발생했습니다";
  if (err.status === 429) return "요청이 너무 많습니다. 잠시 후 다시 시도해주세요";
  if (err.status === 502 || err.status === 503) return "서비스가 일시적으로 응답하지 않습니다";
  if (err.status === 400) return "채팅 내용을 입력해주세요";
  return err.message;
}

export default function App() {
  const [chatText, setChatText] = useState("");
  const [legal, setLegal] = useState(false);
  const [loading, setLoading] = useState(false);
  const [result, setResult] = useState(null);
  const [error, setError] = useState(null);

  async function handleSubmit(e) {
    e.preventDefault();
    setLoading(true);
    setError(null);
    setResult(null);
    try {
      const data = await analyzeChat(chatText, legal);
      setResult(data);
    } catch (err) {
      setError(errorMessage(err));
    } finally {
      setLoading(false);
    }
  }

  return (
    <div>
      <h1>judgpt</h1>
      <form onSubmit={handleSubmit}>
        <textarea
          value={chatText}
          onChange={(e) => setChatText(e.target.value)}
          placeholder="채팅 내용을 붙여넣으세요"
          rows={10}
        />
        <div>
          <label>
            <input
              type="checkbox"
              checked={legal}
              onChange={(e) => setLegal(e.target.checked)}
            />
            법률 정보 포함(조문/판례)
          </label>
        </div>
        <button type="submit" disabled={loading}>
          {loading ? "분석 중..." : "분석하기"}
        </button>
      </form>

      {error && <p role="alert">{error}</p>}

      {result && (
        <div>
          {result.expressions.length === 0 ? (
            <p>문제 표현이 발견되지 않았습니다.</p>
          ) : (
            result.expressions.map((expr, i) => (
              <div key={i}>
                <p>"{expr.text}"</p>
                <p>유형: {expr.type}</p>
                <p>위험도: {expr.risk}</p>
                {expr.target && <p>대상: {expr.target}</p>}
                {expr.context && <p>맥락: {expr.context}</p>}
                {expr.applicable_laws && (
                  <p>
                    적용 가능 법률:{" "}
                    {expr.applicable_laws.length > 0
                      ? expr.applicable_laws.join(", ")
                      : "판단 근거 없음"}
                  </p>
                )}
                {expr.related_cases && (
                  <p>
                    관련 판례:{" "}
                    {expr.related_cases.length > 0
                      ? expr.related_cases.join(" / ")
                      : "판단 근거 없음"}
                  </p>
                )}
              </div>
            ))
          )}
        </div>
      )}

      <hr />
      <p>{DISCLAIMER}</p>
    </div>
  );
}
```

- [ ] **Step 6: `frontend/src/main.jsx` 작성**

```jsx
import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
```

- [ ] **Step 7: 의존성 설치**

Run: `cd frontend && npm install`
Expected: `node_modules/` 생성, 에러 없이 완료.

- [ ] **Step 8: 수동 확인 — 백엔드 + 프론트 둘 다 띄워서 브라우저로 확인**

두 터미널에서 각각:

```bash
# 터미널 1 (repo 루트, Ollama가 로컬에 떠 있어야 함)
uvicorn judgpt.web.app:app --reload --port 8000

# 터미널 2
cd frontend && npm run dev
```

브라우저에서 `http://localhost:5173` 접속 후 확인할 것:
1. `tests/fixtures/harmful_example.txt`(`A: 너 진짜 뭐하는 짓이야\nB: 너 같은 새끼는 사회에서 없어져야 한다\nA: 그만해\nB: 사진 뿌려버린다`) 내용을 붙여넣고 분석 → 표현 카드 여러 개, 하단에 고지 문구 표시되는지.
2. "법률 정보 포함" 체크 후 같은 텍스트로 다시 분석 → 적용 가능 법률/관련 판례가 카드에 추가되는지.
3. 입력창 비운 채로 분석 시도 → "채팅 내용을 입력해주세요" 메시지.
4. (선택) Ollama를 잠깐 멈추고 분석 시도 → "서비스가 일시적으로 응답하지 않습니다" 메시지.

- [ ] **Step 9: 커밋**

```bash
git add frontend/
git commit -m "React 프론트엔드(Vite) 추가 — 채팅 입력, 결과 표시, 법률 옵션"
```

---

### Task 6: Dockerfile — 백엔드 + 빌드된 프론트 컨테이너화

**Files:**
- Modify: `judgpt/web/app.py`
- Create: `Dockerfile`
- Create: `.dockerignore`

**Interfaces:**
- Consumes: `frontend/`(Task 5, `npm run build`가 만드는 `frontend/dist/`), `judgpt/web/app.py`의 `app`(Task 1-4)
- Produces: `docker build`로 만들 수 있는 단일 이미지 — 컨테이너 실행 시 `uvicorn`이 API와 빌드된 프론트 정적 파일을 같은 포트(8000)에서 함께 서빙

- [ ] **Step 1: `judgpt/web/app.py` 맨 아래에 정적 파일 서빙 추가**

파일 맨 위 import 블록에 아래 두 줄 추가:

```python
import os

from fastapi.staticfiles import StaticFiles
```

파일 맨 아래(마지막 함수 정의 뒤)에 추가:

```python
_frontend_dist = os.path.join(os.path.dirname(__file__), "..", "..", "frontend", "dist")
if os.path.isdir(_frontend_dist):
    app.mount("/", StaticFiles(directory=_frontend_dist, html=True), name="frontend")
```

`app.mount("/", ...)`는 반드시 `@app.post("/api/analyze")` 라우트 정의보다 **뒤**에 와야 한다 — 라우트는 등록 순서대로 매칭되므로, `/api/analyze`가 먼저 등록되어 있어야 정적 파일 마운트가 그 경로를 가로채지 않는다. `frontend/dist`가 없는 로컬 개발 환경(아직 `npm run build`를 안 한 경우)에서는 이 블록이 조용히 스킵된다 — 에러 없이 API만 동작.

- [ ] **Step 2: 테스트 실행 — 기존 테스트 회귀 확인**

Run: `pytest tests/test_web_api.py -v`
Expected: PASS (7 passed) — `frontend/dist`가 없으므로 정적 마운트는 스킵되고 API 동작은 그대로.

- [ ] **Step 3: `.dockerignore` 작성**

```
frontend/node_modules
frontend/dist
.git
.pytest_cache
__pycache__
*.pyc
.superpowers
```

- [ ] **Step 4: `Dockerfile` 작성**

```dockerfile
FROM node:20-slim AS frontend-build
WORKDIR /frontend
COPY frontend/package.json ./
RUN npm install
COPY frontend/ ./
RUN npm run build

FROM python:3.12-slim
WORKDIR /app
COPY pyproject.toml ./
COPY judgpt/ ./judgpt/
RUN pip install --no-cache-dir ".[web]"
COPY --from=frontend-build /frontend/dist ./frontend/dist
EXPOSE 8000
CMD ["uvicorn", "judgpt.web.app:app", "--host", "0.0.0.0", "--port", "8000"]
```

컨테이너 실행 시 `OLLAMA_BASE_URL` 환경변수로 컨테이너 밖(또는 같은 네트워크의 다른 컨테이너) Ollama를 가리키도록 설정해야 한다(예: `docker run -e OLLAMA_BASE_URL=http://host.docker.internal:11434/v1 -p 8000:8000 judgpt-web`) — 실제 배포 방식(단일 호스트 docker-compose, k8s 등)은 이 계획의 범위 밖이다(spec §0).

- [ ] **Step 5: 이미지 빌드 확인**

Run: `docker build -t judgpt-web .`
Expected: 빌드가 에러 없이 완료(exit code 0). 이 환경에 Docker가 설치되어 있지 않으면, 이 스텝은 스킵하고 self-review에 "Docker 미설치로 빌드 검증 못 함"이라고 명시한다 — Dockerfile 문법 자체는 위 내용을 그대로 따른 것이므로 코드 리뷰로 검토 가능.

- [ ] **Step 6: 커밋**

```bash
git add judgpt/web/app.py Dockerfile .dockerignore
git commit -m "Dockerfile 추가 — 백엔드 + 빌드된 프론트 정적 파일 컨테이너화"
```

---

## Self-Review 메모 (계획 작성자용, 실행 시 참고만)

- **스펙 커버리지**: spec §1(배포 전제/LLM 백엔드) → Task 1 dependencies.py 싱글턴, §2(아키텍처) → Task 1-3 전체, §3(API 계약) → Task 1(200/400) · Task 2(legal) · Task 3(503) · Task 4(429/502는 Task 3에서 이미 커버), §4(Rate limiting) → Task 4, §5(프론트엔드) → Task 5, §6(테스트 전략) → 각 태스크의 Test 섹션 + Task 5 Step 8(수동 확인), §7(파일 구조) → 본 계획의 File Structure와 동일, §8(Global Constraints) → 본 계획 상단에 그대로 반영.
- **타입 일관성**: `AnalyzeRequest`(Task 1 정의) 필드가 Task 2-4에서 그대로 사용됨. `get_llm`/`get_embedder`(Task 1 정의)가 Task 2-4에서 동일 시그니처로 import·override됨. `analyze_endpoint`의 반환 타입(`AnalysisResult | EnrichedResult`)이 Task 2에서 도입된 이후 Task 3-4에서 그대로 유지됨. `limiter`(Task 4 정의)가 conftest.py에서 import되는 이름과 일치.
- **플레이스홀더**: 없음 — 모든 코드/설정 파일 내용이 완전한 형태로 각 스텝에 포함됨. Dockerfile의 `OLLAMA_BASE_URL` 예시 값은 실제로 동작하는 값(`host.docker.internal`)이고, 실제 배포 상세는 spec §0에서 명시적으로 범위 밖으로 뺀 것이라 placeholder가 아니라 의도된 범위 제한이다.
